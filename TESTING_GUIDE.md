# Инструкция по тестированию новой системы

## Подготовка (на WSL ПК)

### 1. Перенос кода
Убедитесь, что все новые файлы перенесены:
- `scripts/init_metadata_db.py`
- `src/metadata_manager.py`
- Обновлённые: `src/docs_parser.py`, `src/app.py`
- Документация: `USAGE_GUIDE.md`, `TESTING_GUIDE.md`
- Тестовый файл: `test_instructions.md`

### 2. Инициализация базы метаданных
```bash
cd /path/to/RW_Search
python scripts/init_metadata_db.py
```

**Ожидаемый результат:**
```
База данных метаданных создана: /path/to/RW_Search/data/metadata.db
Добавлено 10 предустановленных тегов
```

**Проверка:**
```bash
ls -la data/
# Должен появиться файл metadata.db
```

### 3. Проверка структуры БД (опционально)
```bash
sqlite3 data/metadata.db
```

В SQLite консоли:
```sql
.tables
-- Должно показать: instructions, tags, instruction_tags, instruction_images, instruction_history

SELECT * FROM tags;
-- Должно показать 10 предустановленных тегов

.quit
```

## Тест 1: Загрузка одного файла с одной инструкцией

### Создать тестовый файл
`data/docs/test_single.md`:
```markdown
Это тестовая инструкция по настройке ЕГАИС.

Шаги:
1. Открыть настройки
2. Выбрать ЕГАИС
3. Настроить параметры
```

### Действия в UI
1. Запустить приложение: `streamlit run src/app.py --server.port 8502`
2. Перейти на вкладку "📄 Загрузка документов"
3. Загрузить `test_single.md`
4. Выбрать тип: **"Один файл = одна инструкция"**
5. Автор: оставить "Admin"
6. Теги: выбрать `ЕГАИС`, `настройка`
7. Нажать "📤 Загрузить документ"

### Ожидаемый результат
```
Найдено инструкций: 1
Обработка: test_single
✓ test_single (N чанков)
🎉 Загрузка завершена! Добавлено инструкций: 1
```

### Проверка в БД
```bash
sqlite3 data/metadata.db "SELECT title, source_type, tags FROM instructions JOIN instruction_tags ON instructions.id = instruction_tags.instruction_id JOIN tags ON instruction_tags.tag_id = tags.id WHERE title = 'test_single';"
```

Должно показать: `test_single|single_file|ЕГАИС` и т.д.

## Тест 2: Загрузка множественных инструкций

### Использовать готовый файл
Файл уже создан: `test_instructions.md` с 3 инструкциями

### Действия в UI
1. Перейти на вкладку "📄 Загрузка документов"
2. Загрузить `test_instructions.md`
3. Выбрать тип: **"Множество инструкций (разделитель ---)"**
4. Автор: "Admin"
5. Теги: выбрать `ЕГАИС`, `1С`, `отчет`
6. Нажать "📤 Загрузить документ"

### Ожидаемый результат
```
Найдено инструкций: 3
Обработка: Настройка фильтра ЕГАИС
✓ Настройка фильтра ЕГАИС (N чанков)
Обработка: Решение ошибки подключения к 1С
✓ Решение ошибки подключения к 1С (N чанков)
Обработка: Создание отчета по продажам
✓ Создание отчета по продажам (N чанков)
🎉 Загрузка завершена! Добавлено инструкций: 3
```

### Проверка
```bash
sqlite3 data/metadata.db "SELECT COUNT(*) FROM instructions;"
# Должно быть: 4 (1 из теста 1 + 3 из теста 2)
```

## Тест 3: Просмотр базы знаний

### Действия в UI
1. Перейти на вкладку "📊 База знаний"
2. Проверить статистику:
   - Активных инструкций: 4
   - Неактуальных: 0
   - Всего тегов: 10 (или больше, если добавляли свои)

3. Проверить фильтрацию по тегу:
   - Выбрать "ЕГАИС" → должна показаться только 1 инструкция из теста 2
   - Выбрать "1С" → только инструкция про ошибку 1С
   - Выбрать "Все" → все 4 инструкции

### Ожидаемый результат
Все инструкции отображаются с:
- ID (UUID)
- Путь к файлу
- Автор
- Дата создания
- Тип (single_file или multi_instruction)
- Теги

## Тест 4: Пометка неактуальной

### Действия в UI
1. В "📊 База знаний" найти инструкцию "test_single"
2. Нажать кнопку "Пометить неактуальной"

### Ожидаемый результат
- Сообщение: "Помечена как неактуальная"
- Статистика обновилась: Активных: 3, Неактуальных: 1
- Иконка изменилась: ❌ test_single

### Проверка
```bash
sqlite3 data/metadata.db "SELECT title, active FROM instructions WHERE title = 'test_single';"
# Должно показать: test_single|0
```

### Восстановление (опционально)
Включить чекбокс "☑ Показать неактуальные" — должна появиться инструкция с ❌

## Тест 5: Удаление инструкции

### Действия в UI
1. В "📊 База знаний" найти инструкцию для удаления
2. Нажать "🗑️ Удалить"

### Ожидаемый результат
- Сообщение: "Удалена"
- Инструкция исчезла из списка
- Статистика обновилась

### Проверка
```bash
sqlite3 data/metadata.db "SELECT COUNT(*) FROM instructions;"
# Должно уменьшиться на 1
```

## Тест 6: Поиск с новыми метаданными

### Действия в UI
1. Перейти на вкладку "🔍 Поиск"
2. Ввести запрос: **"Как настроить фильтр ЕГАИС?"**
3. Нажать "🔍 Найти"

### Ожидаемый результат
- LLM должен найти инструкцию "Настройка фильтра ЕГАИС"
- В источниках должна отображаться эта инструкция с правильными метаданными:
  - Название: "Настройка фильтра ЕГАИС"
  - Теги должны быть видны в метаданных чанка

### Важно проверить
Запрос по **названию** должен работать лучше, чем раньше, потому что:
- Название добавляется в каждый чанк (`prepare_text_for_chunking`)
- Эмбеддинги содержат название

### Дополнительный тест
Запрос: **"ошибка 1С"**
- Должна найтись инструкция "Решение ошибки подключения к 1С"

## Тест 7: Добавление пользовательских тегов

### Действия в UI
1. Загрузка документов
2. В поле "Дополнительные теги" ввести: `тест_2025, важное`
3. Загрузить файл

### Ожидаемый результат
- Теги `тест_2025` и `важное` добавлены в БД
- Они появляются в списке фильтров на вкладке "База знаний"

### Проверка
```bash
sqlite3 data/metadata.db "SELECT name FROM tags WHERE name LIKE 'тест%';"
# Должно показать: тест_2025
```

## Возможные проблемы и решения

### Проблема: ModuleNotFoundError: No module named 'src'
**Решение:**
```bash
export PYTHONPATH=/path/to/RW_Search:$PYTHONPATH
python scripts/init_metadata_db.py
```

### Проблема: "no such table: instructions"
**Решение:** Запустите `python scripts/init_metadata_db.py`

### Проблема: Теги не отображаются
**Решение:**
```bash
sqlite3 data/metadata.db "SELECT * FROM tags;"
# Если пусто, запустите init_metadata_db.py заново
```

### Проблема: Кодировка в Windows консоли
**Решение:** Используйте WSL или Git Bash вместо CMD

### Проблема: Инструкции не разделились
**Проверьте:**
1. Выбран тип "Множество инструкций"?
2. Разделитель `---` на отдельной строке?
3. После `---` есть название на первой строке?

## Проверка интеграции с ChromaDB

### Убедитесь что метаданные синхронизированы
```python
from src.storage import get_chroma

client, collection = get_chroma()
results = collection.get(limit=1, include=['metadatas'])
print(results['metadatas'][0])
```

**Ожидаемый результат:**
```python
{
    'instruction_id': 'uuid...',
    'doc_id': 'uuid...',
    'title': 'Настройка фильтра ЕГАИС',
    'filename': 'test_instructions.md',
    'tags': 'ЕГАИС,1С,отчет',
    'author': 'Admin',
    'active': True,
    ...
}
```

## Финальная проверка

Выполните все тесты последовательно и убедитесь:

✅ База данных инициализируется
✅ Загружаются документы обоих типов
✅ Теги работают (выбор и добавление)
✅ База знаний отображается корректно
✅ Фильтрация по тегам работает
✅ Пометка неактуальной работает
✅ Удаление работает
✅ Поиск находит инструкции по названию
✅ Метаданные синхронизированы с ChromaDB

## Что дальше?

После успешного тестирования можно:
1. Начать загрузку реальных инструкций
2. Добавить больше тегов по вашим потребностям
3. Настроить фильтрацию в поиске по тегам (будущая задача)
4. Реализовать извлечение изображений (пункт 5 roadmap)
5. Добавить редактирование инструкций
