# Поддержка изображений в RW_Search

## Обзор

RW_Search теперь поддерживает автоматическое извлечение и отображение изображений из документов (.docx и .md). Изображения автоматически извлекаются при загрузке документа, сохраняются в `data/images/` и отображаются в результатах поиска.

## Поддерживаемые форматы

### 1. Microsoft Word (.docx)
- **Извлечение**: Автоматическое извлечение встроенных изображений
- **Форматы**: PNG, JPG, JPEG (сохраняются как PNG)
- **Размещение**: Inline изображения в параграфах

### 2. Markdown (.md)
- **Синтаксис**: `![alt text](path/to/image.png)`
- **Поддержка**: Локальные изображения (относительные пути)
- **Действие**: Копирование изображений в `data/images/`
- **Примечание**: URL изображения (http://, https://) остаются без изменений

## Как это работает

### Процесс обработки

1. **Загрузка документа**
   - Пользователь загружает .docx или .md файл через UI
   - Система определяет тип файла

2. **Извлечение изображений**
   - **.docx**: Извлечение из XML структуры документа
   - **.md**: Поиск по регулярному выражению `!\[.*\]\(.*\)`

3. **Сохранение**
   - Генерация уникального имени: `{doc_id}_img_{counter}.png`
   - Сохранение в `data/images/`
   - Путь: относительный от корня проекта

4. **Замена в тексте**
   - Изображения заменяются на плейсхолдеры
   - Формат: `[[image: data/images/{doc_id}_img_1.png]]`

5. **Индексация**
   - Текст с плейсхолдерами индексируется в ChromaDB
   - Пути к изображениям сохраняются в метаданных

6. **Отображение**
   - При поиске изображения извлекаются из метаданных
   - Отображаются в UI под ответом модели
   - Также доступны в каждом источнике

## Использование

### Загрузка документа с изображениями

```markdown
# Пример Markdown файла

Это инструкция с изображением.

## Скриншот настроек

![Настройки](./images/settings.png)

Следуйте инструкциям на скриншоте.
```

**После загрузки:**
- Изображение `./images/settings.png` копируется в `data/images/`
- В тексте появится: `[[image: data/images/abc123_img_1.png]]`
- LLM увидит плейсхолдер в контексте

### Отображение в UI

**Раздел "Связанные изображения":**
- Отображаются все изображения из найденных документов
- Расположение: 2 изображения в ряд
- Caption с номером изображения

**В источниках:**
- Индикатор 🖼️ если источник содержит изображения
- Изображения отображаются внутри expander'а источника

### Взаимодействие с LLM

LLM получает текст с плейсхолдерами:
```
[[image: data/images/abc123_img_1.png]]
```

**System prompt** инструктирует LLM:
- Упоминать наличие изображений в ответе
- Использовать фразы типа "См. изображение" или "На изображении показано..."

**Пример ответа LLM:**
```
Для настройки фильтра выполните следующие шаги:

1. Откройте раздел настроек (см. изображение для визуального примера)
2. Выберите тип фильтра
3. Сохраните изменения

На изображении показан пример правильной конфигурации.
```

## Технические детали

### Структура файлов

```
data/
├── docs/                      # Исходные документы
│   └── инструкция.docx
└── images/                    # Извлеченные изображения
    ├── abc123_img_1.png
    ├── abc123_img_2.png
    └── def456_img_1.png
```

### Формат метаданных

```python
{
    'instruction_id': 'uuid',
    'doc_id': 'uuid',
    'title': 'Название',
    'images': [
        'data/images/abc123_img_1.png',
        'data/images/abc123_img_2.png'
    ],
    # ... другие поля
}
```

### API изменения

**RAG Pipeline:**
```python
result = rag.query("вопрос пользователя")

# Результат содержит:
{
    'answer': 'ответ LLM',
    'sources': [...],
    'images': [
        'data/images/abc123_img_1.png',
        ...
    ]
}
```

## Примеры использования

### Пример 1: Инструкция с одним изображением

**Файл**: `setup_guide.md`
```markdown
# Руководство по настройке

1. Откройте панель управления
2. Найдите раздел "Настройки"

![Панель настроек](./screenshots/panel.png)

3. Установите необходимые параметры
```

**Результат:**
- Изображение сохранено как `data/images/{doc_id}_img_1.png`
- В тексте: `[[image: data/images/{doc_id}_img_1.png]]`
- Отображается в UI при поиске

### Пример 2: Документ Word с несколькими изображениями

**Файл**: `troubleshooting.docx`
- Параграф 1: Текст
- Параграф 2: Изображение (ошибка)
- Параграф 3: Текст с описанием
- Параграф 4: Изображение (решение)

**Результат:**
- 2 изображения извлечены
- Сохранены как `{doc_id}_img_1.png` и `{doc_id}_img_2.png`
- Плейсхолдеры вставлены в соответствующих местах

### Пример 3: Множество инструкций с изображениями

**Файл**: `multi_instructions.md`
```markdown
Инструкция 1: Настройка фильтра

![Фильтр](./img1.png)

Описание настройки

---

Инструкция 2: Проверка

![Результат](./img2.png)

Описание проверки
```

**Результат:**
- 2 инструкции созданы
- Каждая содержит свое изображение в метаданных
- При поиске показываются только релевантные изображения

## Ограничения

1. **URL изображений**: Внешние URL в Markdown не скачиваются (остаются как есть)
2. **Форматы**: .docx поддерживает только встроенные изображения (не floating)
3. **Размер**: Нет ограничения на размер, но рекомендуется оптимизировать изображения
4. **Multimodal**: LLM не "видит" изображения, только упоминает их наличие

## Решение проблем

### Изображение не отображается

**Проверьте:**
1. Файл существует в `data/images/`
2. Путь в метаданных корректный (относительный от корня)
3. Формат изображения поддерживается Streamlit

### Изображение не извлеклось из .md

**Возможные причины:**
1. Путь к изображению некорректный (файл не найден)
2. Синтаксис Markdown неправильный
3. URL изображение (не копируется)

### Изображение не извлеклось из .docx

**Возможные причины:**
1. Изображение не inline (floating)
2. Формат изображения не поддерживается
3. Поврежденная структура документа

## Тестирование

Запустите тестовый скрипт:

```bash
python test_image_processing.py
```

**Проверяет:**
- Извлечение из Markdown
- Извлечение нескольких изображений
- Множественные инструкции
- Сохранение файлов
- Плейсхолдеры в тексте

## Будущие улучшения

- [ ] Скачивание изображений по URL
- [ ] Поддержка других форматов (.pdf, .html)
- [ ] Сжатие изображений для экономии места
- [ ] Multimodal LLM для анализа содержимого изображений
- [ ] OCR для текста на изображениях
- [ ] Поддержка GIF и видео
