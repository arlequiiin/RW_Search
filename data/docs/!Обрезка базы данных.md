22:00 заходим на эталон, обновляем конфигурацию и прогружаем цены, ДК и ТМЦ

23:00 на эталоне начинаем делать базы с остатками (открываем конфигуратор базы эталон, запускаем поверх 1с – в 1с заходим в обработки – обработки прочее-превращаем эталонную базу в новую, галочка на остатках должна быть, в константах проверяем номер магазина. 

Закрываем 1с и в конфигураторе во вкладке администраторы нажимаем выгрузить. Все архивы бд с остатками сохраняются на диск С, оттуда копируем их на свой пк), после открываем магазины под кассой(в тени) и начинаем закидывать папку “обрезка”, после закидываем в нее базы с остатками, проверяем на магазине, что прогружены цены(после 22:00, если нет то прогружаем самостоятельно), а также проверяем чтобы были выгружены ддс, ко, документы. 

==В 1С RW проверяем выгрузку с магазинов 1с rw-продажа-и должны быть документы за сегодняшнее число за 20:00, если выгрузка появилась, то проверяем бекап базы rozn за 23:00(обрезку начинаем после 23:00 на магазине), потом запускаем батник “для обрезки бд”, после его отработки закидываем в архив с базой и остатками файл userdef (после его отработки файл должен быть на диске D\USERDEF, если его нет, то его можно взять с D\BASE\USERDEF), после запускаем батник “Обрезка базы данных (консольное)”==, либо вручную заходим в sql management studio (sa 159963), удаляем rozn и rozn old, после создаем их заново(при создании путь должен быть D:\SQLDATA размер 64мб, во второй вкладке должна стоять галочка на перезапись, если разворачиваем бекап), rozn оставляем пустую, а в roznold разворачиваем бекап за 23:00. 
После восстановления roznold, заходим в конфигуратор старой бд в 1с и во вкладке администрирование редактируем rozn на roznold, после заходим в 1с старой бд – константы – и удаляем цифру напротив константы “признак настроенности тсд”, также проверяем чтобы были продажи. 
Заходим в конфигуратор новой базы и загружаем архив базы с остатками(с уже подкинутым файлом userdef(иначе не будет пользователей)), после заходим в 1с и выходим(чтобы открыть период) и снова заходим только в 1с(новую бд), там загружаем дк цены тмц, потом ==через обработки загружаем остатки марок==(сейчас загружается автоматически, но если нужен архив, то запускаем батник для выгрузки S-файлов “скачать архив с  марками с WSR010 v.1”, ==загрузка с ФТПДляФО,== ==загрузка остатков и номеров документа==(документы загружаем со своего пк, в 1с rw заходим в операции – обработки – выгрузка остатков и номеров документов для восстановления магазина, пишем номер магазина и нажимаем загрузить, сохранятся на диск С). 
Далее нам нужно открыть старый доступ (старый удаленный доступ) – открыть комп администратора для всех магазинов и на каждом в тени запустить батник NEW_запустить_на_админской_кассе_при_обрезке, после проверяем чтобы у каждой базы был корректный сетевой путь.