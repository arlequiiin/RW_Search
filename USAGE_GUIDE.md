# Руководство по использованию системы управления инструкциями

## Быстрый старт

### 1. Инициализация базы данных

Перед первым использованием нужно инициализировать базу метаданных:

```bash
python scripts/init_metadata_db.py
```

Это создаст:
- База данных `data/metadata.db`
- Таблицы для инструкций, тегов, изображений
- 10 предустановленных тегов (ЕГАИС, 1С, фильтр, ошибка, и др.)

### 2. Запуск приложения

```bash
streamlit run src/app.py --server.port 8502
```

## Загрузка инструкций

### Два типа документов

#### Тип 1: Один файл = одна инструкция
- Название файла становится названием инструкции
- Весь текст файла — это одна инструкция
- Подходит для: отдельных больших инструкций с картинками

**Пример:**
Файл: `Настройка_ЕГАИС_фильтра.docx`
→ Инструкция: "Настройка_ЕГАИС_фильтра"

#### Тип 2: Множество инструкций (разделитель `---`)
- Один файл содержит несколько инструкций
- Разделитель между инструкциями: `---`
- Первая строка после `---` = название инструкции
- Остальной текст = содержимое инструкции

**Пример файла:**
```markdown
Настройка фильтра ЕГАИС

Инструкция по настройке...
1. Шаг 1
2. Шаг 2

---

Решение ошибки подключения

Если возникла ошибка...
1. Проверить настройки
2. Перезапустить службу

---

Создание отчета

Для создания отчета...
```

Из этого файла будет создано **3 отдельные инструкции**.

### Работа с тегами

#### Предустановленные теги
- ЕГАИС
- 1С
- фильтр
- ошибка
- настройка
- отчет
- установка
- обновление
- импорт
- экспорт

#### Как добавлять теги при загрузке

1. **Выбрать из списка** — используйте multiselect для выбора существующих тегов
2. **Добавить свои** — введите новые теги через запятую в поле "Дополнительные теги"

**Пример:**
- Выбрано из списка: `ЕГАИС`, `фильтр`
- Дополнительно: `настройка_2024, важное`
- Итого теги: `ЕГАИС`, `фильтр`, `настройка_2024`, `важное`

### Процесс загрузки

1. Откройте вкладку **"📄 Загрузка документов"**
2. Выберите файл (.docx, .md, .txt)
3. Выберите тип документа:
   - "Один файл = одна инструкция"
   - "Множество инструкций (разделитель ---)"
4. Укажите автора
5. Выберите теги из списка
6. Добавьте дополнительные теги (если нужно)
7. Нажмите **"📤 Загрузить документ"**

Система:
- Распарсит файл на инструкции
- Разобьёт каждую на чанки
- Создаст эмбеддинги
- Сохранит в ChromaDB и metadata.db
- Покажет прогресс для каждой инструкции

## Управление базой знаний

### Просмотр инструкций

Вкладка **"📊 База знаний"** показывает:
- Статистику: активные/неактуальные инструкции, количество тегов
- Фильтры:
  - По активности (показать неактуальные)
  - По тегу
- Список всех инструкций с метаданными

### Фильтрация

**По тегу:**
```
Фильтр: ЕГАИС → показывает только инструкции с тегом "ЕГАИС"
```

**По активности:**
```
☑ Показать неактуальные → показывает также помеченные неактуальными
```

### Управление инструкциями

Для каждой инструкции доступны:
- **Пометить неактуальной** — инструкция остаётся в базе, но помечается как устаревшая
- **🗑️ Удалить** — полное удаление инструкции из БД (осторожно!)

## Поиск

### Как работает поиск

1. Введите вопрос в поле поиска
2. Система найдёт релевантные чанки используя:
   - Семантический поиск (эмбеддинги)
   - Название инструкции (добавляется в каждый чанк)
   - Теги (в метаданных)
3. LLM сгенерирует ответ на основе найденных чанков
4. Показываются источники с релевантностью

### Настройки поиска

**Количество результатов (top_k):**
- По умолчанию: 3
- Можно изменить в боковой панели (1-10)
- Больше результатов = более полный контекст, но медленнее

## Структура данных

### ChromaDB
Хранит векторные эмбеддинги чанков с метаданными:
```python
{
    'instruction_id': 'uuid',
    'doc_id': 'uuid',
    'title': 'Название инструкции',
    'filename': 'file.docx',
    'file_path': 'data/docs/file.docx',
    'chunk_index': 0,
    'total_chunks': 5,
    'active': True,
    'author': 'Admin',
    'tags': 'ЕГАИС,фильтр,настройка',
    'created_at': '2025-01-15T10:30:00'
}
```

### metadata.db (SQLite)
Хранит полные метаданные:
- **instructions** — инструкции
- **tags** — справочник тегов
- **instruction_tags** — связь инструкций и тегов (many-to-many)
- **instruction_images** — изображения
- **instruction_history** — история изменений

## Использование программно

### Добавление инструкции через код

```python
from src.docs_parser import parse_document
from src.metadata_manager import MetadataManager

# Парсинг документа
instructions = parse_document(
    file_path='data/docs/example.md',
    source_type='multi_instruction',
    tags=['ЕГАИС', 'фильтр'],
    author='Admin'
)

# Сохранение метаданных
metadata_manager = MetadataManager()
for inst in instructions:
    metadata_manager.add_instruction(
        instruction_id=inst['id'],
        doc_id=inst['doc_id'],
        title=inst['title'],
        file_path=inst['file_path'],
        file_format=inst['file_format'],
        source_type=inst['source_type'],
        separator_index=inst['separator_index'],
        author=inst['author'],
        tags=inst['tags'],
        images=inst['images']
    )
```

### Получение инструкций по тегу

```python
from src.metadata_manager import MetadataManager

mm = MetadataManager()

# Все инструкции с тегом ЕГАИС
egais_instructions = mm.get_instructions_by_tag('ЕГАИС')

for inst in egais_instructions:
    print(f"{inst['title']} - {inst['tags']}")
```

### Статистика

```python
from src.metadata_manager import MetadataManager

mm = MetadataManager()
stats = mm.get_stats()

print(f"Активных: {stats['active_instructions']}")
print(f"Неактуальных: {stats['inactive_instructions']}")
print(f"Всего тегов: {stats['total_tags']}")
```

## Решение проблем

### База данных не инициализирована
```
Ошибка: no such table: instructions
```
**Решение:** Запустите `python scripts/init_metadata_db.py`

### Не видно тегов в UI
**Решение:** Проверьте что база инициализирована и содержит предустановленные теги

### Инструкция не разделилась на части
**Решение:**
- Проверьте что выбран тип "Множество инструкций"
- Убедитесь что используется разделитель `---` (три дефиса)
- Проверьте что после `---` есть название инструкции

## Roadmap решённых задач

✅ **Пункт 4 (частично):** Название инструкции теперь добавляется в чанки
✅ **Пункт 7:** Реализованы теги для категоризации инструкций
✅ **Пункт 6:** Добавлена возможность посмотреть базу знаний
✅ **Пункт 9:** Добавлена пометка неактуальными и удаление

## Следующие шаги

🔲 Извлечение и работа с изображениями (пункт 5)
🔲 Версионирование инструкций
🔲 Редактирование инструкций
🔲 Фильтрация поиска по тегам
🔲 Дообучение модели на терминах (пункт 8)
